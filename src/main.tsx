import React from 'react'
import ReactDOM from 'react-dom/client'
import QuizManagementSystem from '@/QuizManagementSystem'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QuizManagementSystem />
  </React.StrictMode>,
)
